基于南京理工大学本科毕业设计（论文）撰写格式要求，对比你提供的 `main.pdf` 编译输出文件，发现存在以下格式不符合规范的地方。你可以将以下详细的修改指令提供给另一个AI，以直接对LaTeX代码进行修改：

### 1. 页眉页脚设置 (Header & Footer)
*   **不符合点**：PDF中奇数页页眉缺少左侧文字，偶数页页眉缺少右侧文字。
*   **格式要求**：奇数页眉：左“毕业设计（论文）报告”，右“题目”，奇数页码；偶数页眉：左“章节号和名”，右“毕业设计（论文）报告”，偶数页码。字体统一为小5号宋体。
*   **修改指令**：
    *   引入并配置 `fancyhdr` 宏包。
    *   设置页眉字体：`\zihao{-5}\songti`。
    *   奇数页（Odd pages）：`\fancyhead[L]{毕业设计（论文）报告}`，`\fancyhead[R]{超音速牛奶喷射与时间旅行的概率动力学研究}`。
    *   偶数页（Even pages）：`\fancyhead[L]{\leftmark}`（或提取当前章标题），`\fancyhead[R]{毕业设计（论文）报告}`。

### 2. 目录格式 (Table of Contents)
*   **不符合点**：目录中的一级标题（如“1 绪论”、“致谢”、“参考文献”等）没有加粗，字号与二级标题相同。
*   **格式要求**：目录中一级标题、致谢、参考文献、附录等用 **4号加粗宋体**，其余为 **小4号宋体**。整体行距20磅。
*   **修改指令**：
    *   利用 `tocloft` 或 `titletoc` 宏包，或者在 `ctex` 宏包中配置目录样式。
    *   修改 `chapter` 级别的目录条目格式：`\zihao{4}\bfseries`。
    *   设置目录中各个条目的行间距为固定值 20 磅。

### 3. 各级标题样式与间距 (Headings)
*   **不符合点**：LaTeX 默认的 `chapter` 标题上方通常有较大的空白，且致谢、参考文献对齐方式不一致（PDF中参考文献居左，致谢居中）。
*   **格式要求**：
    *   正文一级标题（章）：小3号加粗宋体，**居左**，段前段后各18磅。
    *   二级标题（条）：4号加粗宋体，**居左**，段前段后各12磅。
    *   三级标题（款）：小4号加粗宋体，**居左**，段前段后各6磅。
    *   摘要、目录、致谢、参考文献、附录等非正文一级标题：3号宋体加粗，**居中**，段前段后各18磅。
*   **修改指令**：
    *   使用 `\ctexset` 重定义章节格式。
    *   例如：`\ctexset{chapter = {format = \zihao{-3}\bfseries\songti\raggedright, beforeskip = 18pt, afterskip = 18pt}}`。
    *   单独重定义参考文献（thebibliography）环境的标题，使其居中（`\centering`）并应用三号字体。

### 4. 封面与封二信息 (Covers)
*   **不符合点**：缺少校徽Logo，且字体字号未严格区分。
*   **格式要求**：
    *   封面需要插入南京理工大学标志Logo。
    *   封面题目：小一号黑体加粗；作者/学号等信息：小二号楷体加粗。
    *   封二（中文）题目：小一号黑体加粗；作者等：小二号楷体加粗；底部日期：小二号宋体加粗。
    *   封二（英文）题目：二号 Times New Roman 加粗。
*   **修改指令**：
    *   在封面 `titlepage` 环境中使用 `\includegraphics` 引入标准校徽图片。
    *   严格按照上述字号要求，在对应文本前添加 `\zihao{-1}\heiti\bfseries`、`\zihao{-2}\kaishu\bfseries` 等指令。

### 5. 图表格式 (Figures & Tables)
*   **不符合点**：PDF中的图题和表题字号看起来与正文一致（小四号）。
*   **格式要求**：正文中表名在表上，图名在图下，字体统一为 **5号宋体**。
*   **修改指令**：
    *   调用 `caption` 宏包全局设置图表标题字体：`\captionsetup{font={zihao{5}, songti}}`。

### 6. 正文行距 (Line Spacing)
*   **不符合点**：需要确保全局正文行距精确符合要求。
*   **格式要求**：正文使用小4号宋体，行距 **20磅（固定值）**。
*   **修改指令**：
    *   在导言区设置精确的固定行距：`\setlength{\baselineskip}{20pt}`，或使用 `setspace` 宏包配合相应倍数（小四为12pt，20/12 ≈ 1.667倍）。